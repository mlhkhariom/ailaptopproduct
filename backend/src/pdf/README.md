# PDF Generation

Invoice and report PDF generation.
Uses: puppeteer or pdfkit for server-side PDF.

Files:
- invoiceTemplate.js — HTML template for GST invoice
- reportTemplate.js — HTML template for reports
- generatePDF.js — Puppeteer-based PDF generator
